const ENDPOINT='http://127.0.0.1:8765/v1/network';
const COLORS={ok:'#38d66b',telemetry:'#31a8ff',warn:'#e6b93d',bad:'#ff5b46',idle:'#607080'};
let snapshot=null,fetching=false;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function cls(status){const s=String(status||'UNKNOWN').toUpperCase();if(['OK','ONLINE','HEALTHY'].includes(s))return['OK',COLORS.ok,'ok'];if(['DEGRADED','WARN','WARNING'].includes(s))return['WARNUNG',COLORS.warn,'warn'];if(['FAILED','ERROR','OFFLINE'].includes(s))return['FEHLER',COLORS.bad,'bad'];return['UNKNOWN',COLORS.idle,'idle'];}
function fresh(){return snapshot?.measuredAt&&Date.now()-new Date(snapshot.measuredAt).getTime()<20000;}
async function poll(){if(fetching)return;fetching=true;try{const r=await fetch(ENDPOINT,{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);snapshot=await r.json();globalThis.KICC_NETWORK_SNAPSHOT=snapshot;dispatchEvent(new CustomEvent('kicc:network-snapshot',{detail:snapshot}));}catch{snapshot=null;globalThis.KICC_NETWORK_SNAPSHOT=null;}finally{fetching=false;decorate();}}
function ensureStyle(){if(document.querySelector('[data-kicc-fritz-overlay-css]'))return;const s=document.createElement('style');s.dataset.kiccFritzOverlayCss='1';s.textContent=`
.klt-node.fritzbox{width:205px;border-color:#365f79;background:linear-gradient(180deg,#133047,#0a1a29);z-index:4}.klt-node.fritzbox .fritz-leds{display:flex;gap:5px;flex-wrap:wrap;margin-top:7px}.klt-node.fritzbox .fritz-leds span{display:inline-flex;align-items:center;gap:4px;font-size:9px;color:#8ea2b5}.klt-node.fritzbox .fritz-leds i{width:7px;height:7px;border-radius:50%;background:#607080;box-shadow:0 0 7px currentColor}.klt-node.fritzbox .fritz-leds .ok i{background:${COLORS.ok};color:${COLORS.ok}}.klt-node.fritzbox .fritz-leds .warn i{background:${COLORS.warn};color:${COLORS.warn}}.klt-node.fritzbox .fritz-leds .bad i{background:${COLORS.bad};color:${COLORS.bad}}.klt-node.fritzbox .fritz-leds .idle i{background:${COLORS.idle};color:${COLORS.idle}}
`;document.head.appendChild(s);}
function pathBetween(stage,a,b){const sr=stage.getBoundingClientRect(),ar=a.getBoundingClientRect(),br=b.getBoundingClientRect(),x1=ar.left+ar.width/2-sr.left,y1=ar.top+ar.height/2-sr.top,x2=br.left+br.width/2-sr.left,y2=br.top+br.height/2-sr.top,my=(y1+y2)/2;return `M ${x1} ${y1} C ${x1} ${my}, ${x2} ${my}, ${x2} ${y2}`;}
function addPath(svg,d,status,type='NETWORK',latencyMs=null){const [,,c]=cls(status),p=document.createElementNS('http://www.w3.org/2000/svg','path');p.setAttribute('d',d);p.setAttribute('class',`klt-link ${c==='ok'?'active-ok':c==='warn'?'active-warn':c==='bad'?'active-bad':''}`);p.dataset.fritzOverlay='1';svg.appendChild(p);if(!fresh()||c==='idle')return;const color=c==='ok'?COLORS.ok:c==='warn'?COLORS.warn:COLORS.bad,circle=document.createElementNS('http://www.w3.org/2000/svg','circle');circle.setAttribute('r','6');circle.setAttribute('fill',color);circle.setAttribute('class','klt-pulse');circle.dataset.fritzOverlay='1';const anim=document.createElementNS('http://www.w3.org/2000/svg','animateMotion');const dur=Number.isFinite(Number(latencyMs))?Math.max(1.4,Math.min(4,Number(latencyMs)/30)):2.4;anim.setAttribute('dur',`${dur}s`);anim.setAttribute('repeatCount','indefinite');anim.setAttribute('path',d);circle.appendChild(anim);svg.appendChild(circle);}
function tooltipData(){const r=snapshot?.router||{},w=snapshot?.wan||{},i=snapshot?.internet||{};return [['Gerät',r.model||r.name||'FRITZ!Box'],['FRITZ!OS',r.firmware],['Router-IP',r.lanIp],['TR-064',r.tr064===true?'erkannt':r.tr064===false?'nicht erkannt':null],['WAN',w.status],['Öffentliche IP',w.publicIp],['Latenz Router',Number.isFinite(Number(r.latencyMs))?`${r.latencyMs} ms`:null],['Internet-Latenz',Number.isFinite(Number(i.latencyMs))?`${i.latencyMs} ms`:null],['Paketverlust',Number.isFinite(Number(i.packetLossPct))?`${i.packetLossPct} %`:null],['P95',Number.isFinite(Number(i.p95Ms))?`${i.p95Ms} ms`:null],['P99',Number.isFinite(Number(i.p99Ms))?`${i.p99Ms} ms`:null],['Messung',snapshot?.measuredAt?new Date(snapshot.measuredAt).toLocaleString('de-DE'):null]].filter(x=>x[1]!==null&&x[1]!==undefined&&x[1]!=='');}
function bind(node){let tip=document.querySelector('.klt-tooltip');if(!tip)return;node.onmouseenter=()=>{tip.innerHTML=`<strong>FRITZ!Box</strong>${tooltipData().slice(0,9).map(([a,b])=>`<div class="r"><span>${esc(a)}</span><span>${esc(b)}</span></div>`).join('')}<div style="margin-top:7px;color:#8ea2b5">Doppelklick für Internet & Netzwerk</div>`;tip.style.display='block'};node.onmousemove=e=>{tip.style.left=`${Math.min(innerWidth-370,e.clientX+14)}px`;tip.style.top=`${Math.min(innerHeight-300,e.clientY+14)}px`};node.onmouseleave=()=>tip.style.display='none';node.ondblclick=()=>{tip.style.display='none';globalThis.KICC_NAV?.activateTab?.('internet');};}
function decorate(){ensureStyle();const host=document.getElementById('topology');if(!host?.classList.contains('kicc-live-topology'))return;const stage=host.querySelector('.klt-stage'),svg=host.querySelector('.klt-svg'),gateway=host.querySelector('[data-klt-id="gateway"]'),internet=host.querySelector('[data-klt-id="internet"]');if(!stage||!svg||!gateway||!internet)return;host.querySelector('[data-klt-id="fritzbox"]')?.remove();svg.querySelectorAll('[data-fritz-overlay="1"]').forEach(x=>x.remove());
// remove the legacy direct gateway→internet line; it is replaced by the physical FRITZ!Box path.
const staticPaths=[...svg.querySelectorAll('path.klt-link')];const programCount=host.querySelectorAll('[data-klt-id^="program:"]').length,dbCount=host.querySelectorAll('.klt-node.db').length,legacyIndex=programCount+1+dbCount;if(staticPaths[legacyIndex])staticPaths[legacyIndex].remove();
const r=snapshot?.router||{},w=snapshot?.wan||{},isFresh=fresh(),routerState=isFresh?(r.status||'UNKNOWN'):'UNKNOWN',[label,color,state]=cls(routerState),wanState=isFresh?(w.status||snapshot?.internet?.status||'UNKNOWN'):'UNKNOWN',lanState=isFresh?(snapshot?.lan?.status||r.status||'UNKNOWN'):'UNKNOWN';const node=document.createElement('div');node.className='klt-node fritzbox';node.dataset.kltId='fritzbox';node.style.left='52%';node.style.top='63%';node.innerHTML=`<div class="title"><span class="icon">▣</span><span>${esc(r.model||'FRITZ!Box')}</span></div><div class="meta">Router / Firewall / WAN${r.firmware?`<br>FRITZ!OS ${esc(r.firmware)}`:''}</div><div class="status"><span style="color:${color}"><i class="klt-dot"></i>${label}</span><b>${Number.isFinite(Number(r.latencyMs))?`${Math.round(Number(r.latencyMs))} ms`:'—'}</b></div><div class="fritz-leds"><span class="${cls(lanState)[2]}"><i></i>LAN</span><span class="${cls(wanState)[2]}"><i></i>WAN</span><span class="${r.tr064===true?'ok':'idle'}"><i></i>TR-064</span></div>`;stage.appendChild(node);bind(node);
requestAnimationFrame(()=>{const d1=pathBetween(stage,gateway,node),d2=pathBetween(stage,node,internet);addPath(svg,d1,lanState,'LAN',r.latencyMs);addPath(svg,d2,wanState,'WAN',snapshot?.internet?.latencyMs);});}
function start(){poll();setInterval(poll,5000);setInterval(decorate,1200);addEventListener('kicc:program-flow-ingested',()=>setTimeout(decorate,50));addEventListener('kicc:tabchange',e=>{if(e.detail?.tab==='dashboard')setTimeout(decorate,150)});addEventListener('resize',()=>setTimeout(decorate,150));}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();globalThis.KICC_FRITZBOX_TOPOLOGY={poll,decorate,get snapshot(){return snapshot}};
