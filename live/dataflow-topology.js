const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const seen=new Map();
function recent(ts,ms=45000){const t=ts?new Date(ts).getTime():0;return Number.isFinite(t)&&Date.now()-t<=ms;}
function statusClass(s){return({ONLINE:'ok',HEALTHY:'ok',DEGRADED:'warn',WARNING:'warn',FAILED:'bad',OFFLINE:'bad',MAINTENANCE:'blue'})[String(s||'UNKNOWN').toUpperCase()]||'unknown';}
function injectCss(){if(document.getElementById('kicc-flow-topology-css'))return;const s=document.createElement('style');s.id='kicc-flow-topology-css';s.textContent=`
.flow-topology{border:1px solid #26384e;border-radius:14px;background:linear-gradient(180deg,#0d1a2b,#091421);padding:10px;margin-bottom:10px;overflow:hidden}.flow-topology-head{display:flex;justify-content:space-between;gap:12px;align-items:center;margin-bottom:8px}.flow-topology-head strong{font-size:12px}.flow-topology-head span{font-size:9px;color:#8fa2b7}.flow-stage-labels{display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;color:#64748b;font-size:8px;text-transform:uppercase;letter-spacing:.08em;margin:0 4px 5px}.flow-stage-labels span:nth-child(2){text-align:center}.flow-stage-labels span:last-child{text-align:right}.flow-canvas{position:relative;min-height:330px}.flow-links{position:absolute;inset:0;width:100%;height:100%;pointer-events:none;overflow:visible}.flow-line{fill:none;stroke:#334155;stroke-width:1.7;opacity:.8}.flow-line.ok{stroke:#33c481}.flow-line.warn{stroke:#f4b740}.flow-line.bad{stroke:#ef5b5b}.flow-line.blue{stroke:#4da3ff}.flow-line.active{stroke-width:2.4;filter:drop-shadow(0 0 4px currentColor)}.flow-dot{r:4;filter:drop-shadow(0 0 5px currentColor)}.flow-dot.ok{fill:#33c481;color:#33c481}.flow-dot.warn{fill:#f4b740;color:#f4b740}.flow-dot.bad{fill:#ef5b5b;color:#ef5b5b}.flow-dot.blue{fill:#4da3ff;color:#4da3ff}.flow-nodes{position:absolute;inset:0;display:grid;grid-template-columns:1fr 1fr 1fr;gap:12px;pointer-events:none}.flow-col{display:flex;flex-direction:column;justify-content:center;gap:7px}.flow-col:nth-child(2){align-items:center}.flow-col:nth-child(3){align-items:flex-end}.flow-node{width:min(220px,92%);padding:7px 9px;border:1px solid #26384e;border-radius:10px;background:#0f1c2c;box-shadow:0 5px 14px rgba(0,0,0,.14);pointer-events:auto}.flow-node strong{display:block;font-size:9px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.flow-node small{display:block;margin-top:2px;color:#8fa2b7;font-size:7px}.flow-node .flow-state{display:inline-block;width:7px;height:7px;border-radius:50%;margin-right:5px;background:#64748b}.flow-node .flow-state.ok{background:#33c481;box-shadow:0 0 7px #33c481}.flow-node .flow-state.warn{background:#f4b740}.flow-node .flow-state.bad{background:#ef5b5b}.flow-node .flow-state.blue{background:#4da3ff}.flow-legend{display:flex;gap:12px;flex-wrap:wrap;margin-top:7px;color:#8fa2b7;font-size:8px}.flow-legend b{font-weight:600}.flow-key{display:inline-block;width:12px;height:3px;border-radius:3px;margin-right:4px;vertical-align:middle;background:#334155}.flow-key.active{background:#33c481}.flow-key.warn{background:#f4b740}.flow-key.bad{background:#ef5b5b}@media(max-width:800px){.flow-canvas{min-height:410px}.flow-node{width:96%}.flow-nodes{gap:6px}.flow-node strong{font-size:8px}}
`;document.head.appendChild(s);}
function programs(){return globalThis.KICC_PROGRAMS?.programs||[];}
function devices(){try{return globalThis.KICC_DEVICES?.list?.()||[];}catch{return[];}}
function dbs(){return globalThis.KICC?.databases||[];}
function mirror(){return globalThis.KICC_MIRROR?.flow||{};}
function programName(id){return programs().find(p=>p.id===id)?.name||programs().find(p=>p.id===id)?.displayName||id||'Programm';}
function node(id,label,sub,status){return `<div class="flow-node" data-flow-node="${esc(id)}"><strong><i class="flow-state ${statusClass(status)}"></i>${esc(label)}</strong><small>${esc(sub||'')}</small></div>`;}
function buildModel(){
 const ds=devices();const databases=dbs();const mids=new Map();const edges=[];
 for(const d of ds){if(d.programId&&!mids.has(d.programId))mids.set(d.programId,{id:`p:${d.programId}`,label:programName(d.programId),sub:'Programm / Runtime',status:d.status||'UNKNOWN'});}
 if(!mids.has('kicc'))mids.set('kicc',{id:'p:kicc',label:'KICC',sub:'Control Center',status:globalThis.KICC?.systemHealth?.()?.status||'UNKNOWN'});
 const left=ds.map(d=>({id:`d:${d.deviceId}`,label:d.name||d.deviceId,sub:`${d.deviceType||'CLIENT'} · ${d.programId||'—'}`,status:d.status||'UNKNOWN'}));
 const middle=[...mids.values()];
 const right=databases.map(d=>({id:`db:${d.id}`,label:d.name,sub:`${d.role||''} · ${d.provider||''}`,status:d.status||d.health||'UNKNOWN'}));
 for(const d of ds){if(!d.programId)continue;const active=recent(d.measuredAt,45000);edges.push({from:`d:${d.deviceId}`,to:`p:${d.programId}`,kind:'Heartbeat',active,status:d.status||'UNKNOWN',stamp:d.measuredAt});if(['kc-dp2','kc-communication','kicc'].includes(d.programId)&&right.some(x=>x.id==='db:db-supabase-core'))edges.push({from:`p:${d.programId}`,to:'db:db-supabase-core',kind:'Heartbeat/Telemetry',active,status:d.status||'UNKNOWN',stamp:d.measuredAt});}
 const kicc='p:kicc';for(const d of databases){if(d.measuredAt)edges.push({from:kicc,to:`db:${d.id}`,kind:'Messung',active:recent(d.measuredAt,45000),status:d.status||d.health||'UNKNOWN',stamp:d.measuredAt});}
 const m=mirror();if(right.some(x=>x.id==='db:db-supabase-core')&&right.some(x=>x.id==='db:db-neon-core-mirror'))edges.push({from:'db:db-supabase-core',to:'db:db-neon-core-mirror',kind:'Mirror',active:recent(m.lastSuccessAt||m.measuredAt,90000),status:m.status||globalThis.KICC_MIRROR?.health?.()?.status||'UNKNOWN',stamp:m.lastSuccessAt||m.measuredAt});
 return{left,middle,right,edges};
}
function path(a,b){const x1=a.right,x2=b.left,y1=a.cy,y2=b.cy,dx=Math.max(42,(x2-x1)*.48);return `M ${x1} ${y1} C ${x1+dx} ${y1}, ${x2-dx} ${y2}, ${x2} ${y2}`;}
function draw(){const host=document.getElementById('kiccFlowTopology');if(!host)return;const m=buildModel();host.innerHTML=`<div class="flow-topology-head"><strong>Live-Datenströme</strong><span>Clients → Programme → Datenbanken / Services</span></div><div class="flow-stage-labels"><span>Clients & Geräte</span><span>Programme</span><span>Datenbanken & Dienste</span></div><div class="flow-canvas"><svg class="flow-links"></svg><div class="flow-nodes"><div class="flow-col">${m.left.length?m.left.map(n=>node(n.id,n.label,n.sub,n.status)).join(''):node('none-client','Noch keine Remote-Clients','Heartbeats stehen aus','UNKNOWN')}</div><div class="flow-col">${m.middle.map(n=>node(n.id,n.label,n.sub,n.status)).join('')}</div><div class="flow-col">${m.right.map(n=>node(n.id,n.label,n.sub,n.status)).join('')}</div></div></div><div class="flow-legend"><span><i class="flow-key"></i>bekannter Weg / kein aktueller Traffic</span><span><i class="flow-key active"></i>bestätigte aktuelle Aktivität</span><span><i class="flow-key warn"></i>degradiert/verzögert</span><span><i class="flow-key bad"></i>Fehler</span></div>`;
 requestAnimationFrame(()=>{const svg=host.querySelector('.flow-links'),box=host.querySelector('.flow-canvas').getBoundingClientRect();if(!svg||!box.width)return;svg.setAttribute('viewBox',`0 0 ${box.width} ${box.height}`);const coords={};host.querySelectorAll('[data-flow-node]').forEach(el=>{const r=el.getBoundingClientRect();coords[el.dataset.flowNode]={left:r.left-box.left,right:r.right-box.left,cy:r.top-box.top+r.height/2};});let defs='<defs></defs>',body='';m.edges.forEach((e,i)=>{const a=coords[e.from],b=coords[e.to];if(!a||!b)return;const d=path(a,b),c=statusClass(e.status),active=e.active===true;body+=`<path id="flowPath${i}" class="flow-line ${c} ${active?'active':''}" d="${d}"><title>${esc(e.kind)}</title></path>`;if(active)body+=`<circle class="flow-dot ${c}" r="4"><animateMotion dur="${e.kind==='Mirror'?'1.8':'1.3'}s" repeatCount="indefinite" path="${d}"/></circle>`;});svg.innerHTML=defs+body;});
}
function mount(){injectCss();const panel=document.querySelector('[data-kicc-panel="live"]');if(!panel)return;let host=document.getElementById('kiccFlowTopology');if(!host){host=document.createElement('section');host.id='kiccFlowTopology';host.className='flow-topology';const first=panel.querySelector('#kiccLiveConsole')||panel.querySelector('.panel');panel.insertBefore(host,first);}draw();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount,{once:true});else mount();setInterval(draw,5000);addEventListener('resize',()=>requestAnimationFrame(draw));addEventListener('kicc:tabchange',e=>{if(e.detail?.tab==='live')setTimeout(draw,60)});addEventListener('kicc:program-heartbeat-ingested',draw);globalThis.KICC_DATAFLOW_TOPOLOGY={draw,mount,model:buildModel};
