import { runFinalRegression } from './final-regression.js';
const state={running:false,last:null,error:null};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function mount(){const smoke=document.getElementById('runtimeSmoke');if(!smoke)return null;let host=document.getElementById('finalRegression');if(host)return host;host=document.createElement('div');host.id='finalRegression';host.style.marginTop='14px';smoke.insertAdjacentElement('afterend',host);return host;}
function render(){const host=mount();if(!host)return;const r=state.last,rows=r?r.results.map(x=>`<tr><td>${x.ok?'✓':'✕'}</td><td>${esc(x.id)}</td><td>${esc(x.detail)}</td></tr>`).join(''):'';host.innerHTML=`<div class="security-subhead"><strong>Abschluss-Regression / TÜV</strong><span>Konsolidierter Wahrheits-, Mirror-, Netzwerk- und UI-Test</span></div><div class="explorer-policy"><div>${r?`<strong>${esc(r.status)} · ${r.passed}/${r.total}</strong><span>${esc(r.measuredAt)}</span>`:'<strong>NOCH NICHT AUSGEFÜHRT</strong><span>read-only</span>'}</div><button type="button" data-final-run ${state.running?'disabled':''}>${state.running?'TÜV läuft …':'Abschluss-TÜV starten'}</button>${state.error?`<small>${esc(state.error)}</small>`:''}</div>${r?`<div class="table-wrap"><table><thead><tr><th>Status</th><th>Prüfung</th><th>Aussage</th></tr></thead><tbody>${rows}</tbody></table></div>`:''}`;host.querySelector('[data-final-run]')?.addEventListener('click',run);}
async function run(){state.running=true;state.error=null;render();try{state.last=await runFinalRegression()}catch(e){state.error=e?.message||String(e);state.last=null}state.running=false;render();return state.last;}
function boot(){render();setTimeout(run,1600);addEventListener('kicc:tabchange',e=>{if(e.detail?.tab==='tests'&&!state.running)setTimeout(run,120)});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
globalThis.KICC_FINAL_REGRESSION_UI={state,run,render};
